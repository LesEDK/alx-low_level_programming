#include "main.h"
/**
 * _isdigit - function that checks for uppercase
 *
 * @c: integer that is being checked
 * Return: 1 if c is a digit and returns 0 if otherwise
 */
int _isdigit(int c)
{
if (c <= '9')
return (1);
else
return (0);
}
