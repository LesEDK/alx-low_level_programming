#include "main.h"
#include <string.h>
/**
 * _strlen - Fuction that returns the length of a string
 * @s: pointer being checked
 * Return: The length of a string
 */
int _strlen(char *s)
{
return (strlen(s));
}
