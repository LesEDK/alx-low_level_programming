#include <stdio.h>
/**
 * main - Program that prints the lowercase in reverse
 * Return: Always 0 (Success)
 */
int main(void)
{
	char b;

	b = 'z';
	while
		(b >= 'a') {
			putchar(b);
			b--;
		}
	putchar('\n');
	return (0);
}
