#include <stdio.h>
/**
 * main - Program that prints the alphabet in lowercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	char b;

	b = 'a';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
